<main>
  <section class="top-hero">
    <div class="page-container hero-row">
      <div class="hero-text">
        <h1>Bad request</h1>
        <p class="sub">{{#if errorMessage}}{{errorMessage}}{{else}}{{message}}{{/if}}</p>
        <a class="button-link" href="/">Back to Home</a>
      </div>
      <div class="hero-image">
        <div class="image-placeholder">Please go back and double-check the information you submitted.</div>
      </div>
    </div>
  </section>
</main>
